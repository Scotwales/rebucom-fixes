# API Configuration
BASE_URL=https://test.rebucom.com/api/v1/

# Test Account (optional - tests will create their own users)
# Only needed for specific multi-role tests
EMAIL=test-user@yopmail.com
PASSWORD=YourPassword123!
USER_TYPE=Customer

# Test Data IDs (update these per environment)
# Only needed for tests that require pre-existing teams
TEST_TEAM_ONE_ID=your-team-id-here
TEST_TEAM_TWO_ID=your-other-team-id-here
TEST_MULTI_ROLE_EMAIL=multi-role-test@yopmail.com
TEST_MULTI_ROLE_PASSWORD=Password1@

# Optional: Disable TLS verification for test environments (NOT RECOMMENDED)
# NODE_TLS_REJECT_UNAUTHORIZED=0
